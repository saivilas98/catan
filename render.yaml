services:
  - type: web
    name: catan
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npx tsx server/index.ts
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
